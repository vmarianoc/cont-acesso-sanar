# API base URL. Leave unset in dev to use the Vite proxy (/api -> localhost:3000).
# VITE_API_URL=https://api.accessplatform.com.br/v1

# Pre-fill the "ID do Condomínio" field on the login screen (from `pnpm seed` output).
# VITE_TENANT_ID=00000000-0000-0000-0000-000000000000
